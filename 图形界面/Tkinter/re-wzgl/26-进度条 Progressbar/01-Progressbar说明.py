"""
length：进度条的长度，默认是100像素

mode：模式
    1. determinate：默认模式，一个指针从起点到终点，适合于知道所需的工作时间
    2. indeterminate：一个指针在起点和终点来回移动，适合于我们不知道工作所需时间

maximun：进度条的最大值，默认是100
name：进度条的名称，供程序参考使用
orient：进度条的防线，默认是水平（HORIZONTAL），还有垂直（VERTICAL）
value：进度条目前值
variable：记录进度条目前的进度值

"""

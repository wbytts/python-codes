
'''
def open(file, mode='r', buffering=None, encoding=None, errors=None, newline=None, closefd=True):

f.read() 把整个文件读入一个字符串
f.read(N) 读取接下来的N个字符
f.readline() 读取接下来的一行（包括 \n）
f.readlines() 读取整个文件到一个字符串列表

f.write(string) 将一个字符串写入到文件
f.writelines(strList) 将列表中的字符串写入到文件

f.close() 手动关闭文件
f.flush() 把输出去缓冲刷入到硬盘中，但不关闭文件

f.seek(N) 把文件位置移动到偏移量N处
'''




'''
元组是一个序列
元组是不可变对象
元组用来表示元素的集合
其他属性和列表类似

元组的关键是它的不可变性，元组提供了一种完整性约束

元组的主要属性：
    任意对象的有序集合
    通过偏移量存取
    属于“不可变序列”
    固定长度、多样性、任意嵌套
    对象引用的数组

元组的不可变性只适用于元祖本身顶层而非其内容
元组是一种对象的简单组合，而列表是一种随时间改变的数据结构
'''
